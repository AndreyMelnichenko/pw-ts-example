export type PaymentInfo = {
        address?: string,
        cardNumber?: string,
        city?: string,
        country?: string,
        cvc?: string,
        expiration?: string,
        phoneNumber?: string,
        postalCode?: string,
        isFail?: boolean
    }
